"""Internal runtime: the agent/team execution engine and its helpers.

Public consumers should import from the leaf modules (e.g.
``src.core._runner.agent.agent.Agent``) or, more commonly, from the
public re-exports under ``src.core`` (TBD). Treat everything under this
package as implementation detail.
"""
