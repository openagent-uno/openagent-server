"""Memory manager: placeholder module.

Session history is now handled by the Claude Agent SDK (resume=session_id).
Long-term memory and knowledge base are handled by the Obsidian vault
via MCPVault MCP.

Scheduled tasks are managed directly via MemoryDB.

This module is kept for backward compatibility but MemoryManager
is no longer used.
"""
